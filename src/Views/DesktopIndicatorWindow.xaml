<Window x:Class="VirtualDesktopIndicator.Views.DesktopIndicatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Desktop Indicator"
        Height="120" Width="120"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    
    <Window.Resources>
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1"
                           Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                           From="0.8" To="1"
                           Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                           From="0.8" To="1"
                           Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        
        <Storyboard x:Key="FadeOutAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="1" To="0"
                           Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        
        <DropShadowEffect x:Key="DropShadowEffect" 
                         BlurRadius="20" 
                         ShadowDepth="8" 
                         Direction="270" 
                         Opacity="0.3"
                         Color="Black"/>
    </Window.Resources>

    <Grid Name="MainGrid">
        <Grid.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1" CenterX="60" CenterY="60"/>
        </Grid.RenderTransform>
        
        <!-- Main Container -->
        <Border Name="MainBorder"
                CornerRadius="20"
                Margin="10"
                Effect="{DynamicResource DropShadowEffect}">
            <Border.Background>
                <SolidColorBrush x:Name="BackgroundBrush" Color="#F8FAFC"/>
            </Border.Background>
            
            <!-- Content Grid -->
            <Grid>
                <!-- Desktop Number (hidden) -->
                <TextBlock Name="DesktopNumberText"
                          Text="1"
                          Visibility="Collapsed"/>
                
                <!-- Desktop Name (main display) -->
                <TextBlock Name="DesktopLabelText"
                          Text="Desktop"
                          FontFamily="Segoe UI"
                          FontSize="16"
                          FontWeight="SemiBold"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Foreground="{DynamicResource TextPrimaryBrush}"
                          TextAlignment="Center"
                          TextWrapping="Wrap"
                          Margin="15"/>
                
                <!-- Accent Ring -->
                <Ellipse Name="AccentRing"
                        Width="85"
                        Height="85"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Stroke="{StaticResource PrimaryBrush}"
                        StrokeThickness="2"
                        Opacity="0.3"/>
            </Grid>
        </Border>
    </Grid>
</Window>